# Stub — implemented in Phase 3.3 (multi-file/manifest support)

from pathlib import Path


def is_processed(file_path: Path, manifest_path: Path) -> bool:
    return False


def record(file_path: Path, manifest_path: Path) -> None:
    pass
